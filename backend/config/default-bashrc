# PocketDev Default Bash Configuration
# This file is sourced for all terminal sessions

# Set a nice colored prompt
export PS1='\[\033[01;32m\]\u@pocketdev\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '

# Configure git if not already done
if [ -n "$GIT_USER_NAME" ] && [ -n "$GIT_USER_EMAIL" ]; then
    git config user.name "$GIT_USER_NAME" 2>/dev/null || true
    git config user.email "$GIT_USER_EMAIL" 2>/dev/null || true
fi

# Enable color support for ls and grep
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# Common aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ..='cd ..'
alias ...='cd ../..'

# Git aliases
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gl='git log --oneline -10'
alias gd='git diff'

# Enable bash completion
if [ -f /etc/bash_completion ] && ! shopt -oq posix; then
    . /etc/bash_completion
fi

# History settings - Task-scoped
# HISTFILE is set by the terminal controller to .pocketdev_task_history
export HISTSIZE=10000
export HISTFILESIZE=20000
export HISTCONTROL=ignoreboth:erasedups
shopt -s histappend

# Immediately append commands to history file
export PROMPT_COMMAND="${PROMPT_COMMAND:+$PROMPT_COMMAND; }history -a"

# Make less more friendly for non-text input files
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# Set editor
export EDITOR=vim

# Add node_modules/.bin to PATH if it exists
if [ -d "./node_modules/.bin" ]; then
    export PATH="./node_modules/.bin:$PATH"
fi

# Welcome message
echo "🚀 PocketDev Terminal Ready"
echo "📁 Working directory: $(pwd)"
if [ -n "$HISTFILE" ] && [ -f "$HISTFILE" ] && [ -s "$HISTFILE" ]; then
    echo "📜 History: $(wc -l < "$HISTFILE" 2>/dev/null || echo "0") commands available"
fi
echo ""